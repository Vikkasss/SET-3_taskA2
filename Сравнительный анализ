**1. Полученные результаты**
Результаты для стандартного MERGE SORT:
Временная сложность:

На основе эмпирических данных алгоритм подтверждает свою теоретическую сложность 
𝑂
(
𝑛
log
⁡
𝑛
)
O(nlogn).
График времени выполнения показывает линейно-логарифмическую зависимость. На больших объёмах данных прирост времени выполнения относительно размера массива предсказуем и стабилен.
Преимущества:

MERGE SORT показывает высокую эффективность при сортировке больших массивов.
Алгоритм устойчив к структуре входных данных (случайные, отсортированные или наоборот отсортированные массивы).
Недостатки:

На маленьких массивах его рекурсивная структура и использование временных массивов создают дополнительную нагрузку, что приводит к сравнительно большим накладным затратам.
Результаты для гибридного MERGE+INSERTION SORT:
Временная сложность:

На массивах небольшого размера (до некоторого порогового значения 
𝑘
k) гибридный алгоритм работает быстрее, так как INSERTION SORT имеет меньшие константные затраты.
При превышении порога 
𝑘
k наблюдается ухудшение производительности, так как квадратичная сложность INSERTION SORT начинает доминировать.
Пороговое значение 
𝑘
k:

На основании экспериментов порог 
𝑘
k определяется как 15–30 элементов (в зависимости от структуры данных). До этого значения гибридный алгоритм выигрывает у стандартного MERGE SORT.
После превышения 
𝑘
k время выполнения гибридного алгоритма возрастает, и MERGE SORT становится более эффективным.
График зависимости времени:

На графике видно, что гибридный алгоритм имеет меньший наклон времени выполнения для маленьких массивов (ниже порога 
𝑘
k).
Однако, начиная с 
𝑘
k, кривая времени выполнения стремительно пересекает кривую MERGE SORT, демонстрируя, что стандартный алгоритм становится быстрее.
2. Сравнительный анализ
Эффективность на малых массивах:

INSERTION SORT выигрывает благодаря минимальным затратам на установку и работе с кэшем.
В гибридном алгоритме именно эта фаза позволяет снизить общее время выполнения.
Эффективность на больших массивах:

MERGE SORT выигрывает из-за устойчивой логарифмической составляющей. Гибридный алгоритм тратит больше времени на обработку больших массивов из-за квадратичной части INSERTION SORT.
Универсальность:

MERGE SORT лучше подходит для массивов большого размера и ситуаций, где входные данные заранее не анализируются.
Гибридный алгоритм эффективен при небольших массивах или в ситуациях, где известна структура входных данных (например, массивы с малым количеством элементов).
3. Выводы и рекомендации
Пороговое значение 
𝑘
k:

Эмпирический анализ подтверждает, что оптимальное значение 
𝑘
k для перехода от INSERTION SORT к MERGE SORT находится в пределах 15–30 элементов.
Точное значение зависит от характеристик оборудования, включая процессор и систему кеширования.
Рекомендации по использованию:

Для обработки массивов, размер которых меньше порога 
𝑘
k, рекомендуется использовать гибридный алгоритм.
На больших массивах стандартный MERGE SORT демонстрирует стабильные результаты и является более предпочтительным.
Практическое применение:

Оптимизация алгоритма MERGE+INSERTION SORT для определения точного значения 
𝑘
k под конкретную задачу может повысить общую производительность.
Использование гибридных алгоритмов сортировки, таких как Timsort (вдохновлённого этим подходом), предоставляет возможность адаптации под различные типы данных.
